version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    container_name: loaniq-backend

  frontend:
    build: ./loaniq
    ports:
      - "3000:3000"
    environment:
      - CHOKIDAR_USEPOLLING=true
    depends_on:
      - backend
    container_name: loaniq-frontend